
#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int raiz(float op1){
    

    return op1 = sqrt(op1);
}

int main(){
    float n;
    float a = 0;
    printf("Numero: \n");
    scanf("%f",&n);
    a = raiz(n);
    printf (" %f \n", a);
    //if(n % a = 0 ) {
//
  //      printf("Raiz perfecta cuadrada de %i es: %i\n",n,raiz(n));

    //}
    //else {

      //  printf("%i tiene raiz cuadrada imperfecta: %i\n",n,raiz(n));

   // }



    return EXIT_SUCCESS;
}
